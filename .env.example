# ============================================
# NextAuth Configuration
# ============================================
# Secret key for NextAuth.js session encryption
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-key-here-min-32-chars

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection URL
# Format: postgresql://user:password@host:port/database?sslmode=require
# For Vercel Postgres: Get from Vercel Dashboard > Storage > Postgres
# For local: postgresql://postgres:password@localhost:5432/gemini_chatbot?sslmode=require
POSTGRES_URL=postgresql://user:password@host:port/database?sslmode=require

# ============================================
# Google Gemini AI Configuration
# ============================================
# Google Generative AI API Key
# Get from: https://aistudio.google.com/app/apikey
# Or use your organization's API key
GOOGLE_GENERATIVE_AI_API_KEY=your-google-gemini-api-key-here

# ============================================
# Monday.com Integration
# ============================================
# Monday.com API Token
# Get from: https://monday.com/developers/apps
# 1. Go to your Monday.com account
# 2. Developer > My Apps > Create new app or use existing
# 3. Copy the API token from the app settings
MONDAY_API_TOKEN=your-monday-api-token-here

# ============================================
# Slack Integration
# ============================================
# Slack Bot Token (starts with xoxb-)
# Get from: https://api.slack.com/apps
# 1. Create a new Slack App or use existing
# 2. Go to OAuth & Permissions
# 3. Add Bot Token Scopes: channels:history, channels:read
# 4. Install app to workspace
# 5. Copy Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token-here

# ============================================
# Google OAuth Configuration (for Google Workspace)
# ============================================
# Google OAuth Client ID
# Get from: https://console.cloud.google.com/apis/credentials
# 1. Go to Google Cloud Console
# 2. Create a new project or select existing
# 3. Enable Google+ API
# 4. Go to Credentials > Create Credentials > OAuth 2.0 Client ID
# 5. Application type: Web application
# 6. Authorized redirect URIs: http://localhost:3000/api/auth/callback/google (dev)
#    and https://yourdomain.com/api/auth/callback/google (production)
# 7. Copy Client ID and Client Secret
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Optional: Restrict sign-in to specific Google Workspace domain
# Example: "example.com" - only users from @example.com can sign in
# Leave empty to allow any Google account
GOOGLE_WORKSPACE_DOMAIN=your-company-domain.com

# ============================================
# MCP Configuration
# ============================================
# Monday.com MCP is enabled by default when MONDAY_API_TOKEN is set
# The MCP wrapper uses direct API integration (no external server needed)

# ============================================
# Monday.com MCP - READ-ONLY MODE
# ============================================
# IMPORTANT: The Monday.com integration is configured in READ-ONLY mode.
# Only read operations (get, list, query) are allowed.
# Write operations (create, update, delete) are automatically blocked.
#
# For maximum security, use a Monday.com API token from a user with "Viewer" role:
# 1. Create a user with "Viewer" role in Monday.com
# 2. Generate API token for that user
# 3. Use that token here
#
# This ensures double protection: code-level filtering + API-level permissions
